" This file is managed by chezmoi. Always edit it with these commands:
" chezmoi edit {{ .chezmoi.targetFile }}
" chezmoi diff
" chezmoi -v apply

" Leader Key
map <Space> <Leader>

" Plugins
call plug#begin()
Plug 'psliwka/vim-smoothie'
Plug 'tpope/vim-surround'
Plug 'preservim/nerdtree'
Plug 'unblevable/quick-scope'
call plug#end()

" go to normal mode by jj
imap jj <Esc>

" Show current line number
set number

" Show relative line numbers
set relativenumber

" highlight search results
set hlsearch

" searching starts immediately when typing
set incsearch

" ignores case by default in search
set ignorecase

" doesn't ignore case when UPPERCASE letters exist in search
set smartcase

" put search results in the middle of the screen
nnoremap n nzz
nnoremap N Nzz

" backspace works normally in insert mode
nnoremap <silent> <backspace> X

" cursor is always in the middle of screen
set scrolloff=999

" easy tab switching via J and K
nmap J gT
nmap K gt

" join lines via Leader-J
nnoremap <Leader>J J

" easy scrolling via Ctrl-J and Ctrl-K
nnoremap <C-j> <C-d>
nnoremap <C-k> <C-u>

" NERDtree
nmap <Leader>l :NERDTree<CR>
