{{ $original := fromJson (include .chezmoi.targetFile) -}}
{{ if eq (hasKey $original "credHelpers") false }}
  {{ $_ := set $original "credHelpers" (dict) }}
{{ end }}
{{ $_ := set $original.credHelpers "008971659647.dkr.ecr.eu-west-1.amazonaws.com" "ecr-login" -}}
{{ $original | toPrettyJson }}